import { CheckActiveContentDirective } from './check-active-content.directive';

describe('CheckActiveContentDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckActiveContentDirective();
    expect(directive).toBeTruthy();
  });
});
