import { LoadYtApiDirective } from './load-yt-api.directive';

describe('LoadYtApiDirective', () => {
  it('should create an instance', () => {
    const directive = new LoadYtApiDirective();
    expect(directive).toBeTruthy();
  });
});
