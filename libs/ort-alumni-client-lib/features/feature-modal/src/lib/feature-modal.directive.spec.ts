import { FeatureModalDirective } from './feature-modal.directive';

describe('FeatureModalDirective', () => {
  it('should create an instance', () => {
    const directive = new FeatureModalDirective();
    expect(directive).toBeTruthy();
  });
});
